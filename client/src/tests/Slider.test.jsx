import React from 'react';
import { render } from '@testing-library/react';
import AppSlider from '../components/archivedComponents/Slider';

test('renders Slider component', () => {
  render(<AppSlider />);
});
